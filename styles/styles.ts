import {StyleSheet} from 'react-native';

export const commonStyles = StyleSheet.create({
  container: {
    marginTop: 15,
  },
  propertyDesc: {
    fontSize: 12,
    color: '#4E4E4E',
  },
  propertyValue: {
    fontSize: 16,
  },
});
